# Server port
PORT=4000

# OPTIONAL: to actually send contact-form messages to your Gmail inbox,
# create a Gmail "App Password" (Google Account > Security > App Passwords)
# and fill these in. If left blank, messages are just saved to
# backend/data/messages.json and you can read them there.
SMTP_USER=
SMTP_PASS=
CONTACT_TO=
